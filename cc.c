#include<stdio.h>
#include<math.h>
#include<stdlib.h>
#include<time.h>
//exo 1

// Pass-by-value
void incrementByValue(int x) {
    x++;
}

// Pass-by-reference
void incrementByReference(int& x) {
    x++;
}
int fillArray () {
    int A [200000];
  for (int i = 0; i < 200000; i++)
{
    printf("fill the array ") ;
    A1[i] = i ; 
    scanf("%d", A1[i]);
    printf("%d", A1[i]);
}
// return -1;
}
 int deletionFromarray(){
  for (int i = 0; i < 200000; i++)
{
   int A[200000];
    A[i] = A[i - 1];
}
return -1;
 }
void timeMeasurement(){
    int clock ;
int clock(clock);
}

int main (){
    int* p = malloc(sizeof(int));
   int A1[] = fillArray(200000);
   int  A2[] = fillArray(20000);
    int clock; 
deletionFromarray(A1, 200000);
deletionFromarray(A2, 200000);
timeMeasurement(A1,clock) ;
timeMeasurement(A2,clock) ;
// exo2


// exo2


return 0 ;
}
//exo 1

printf("pointer value :" "%d\n" , *p);
printf("pointer value :" "%d\n" , *q);

* p = 5 ;
* q = 5 ;

typedef 
 ptr = (int*)malloc(sizeof(int));
 q = (int*)malloc(sizeof(int)) ;

 if (p == NULL || q == NULL)
 {
 printf("memory allocated failed")
 return 0,
 }
 *p = 5;
 *q = 5 ;

 free(p) ; 
